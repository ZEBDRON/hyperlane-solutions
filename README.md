# hyperlane-solutions

PHP project of Hyperlane Solutions

Laravel Framework

For building a Laravel app:

Run command: php artisan serve

Composer
↓
Installs Laravel and PHP libraries

npm
↓
Installs frontend packages

Vite
↓
Compiles CSS & JavaScript

composer install
npm install
npm run dev
php artisan serve
